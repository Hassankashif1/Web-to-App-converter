{
  "name": "wta-build-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local build server for the Web to App Builder WordPress plugin. Receives build jobs, scaffolds a real Capacitor project, runs Gradle, and reports results back to WordPress.",
  "scripts": {
    "dev": "next dev -p 4000",
    "build": "next build",
    "start": "next start -p 4000",
    "worker": "node worker.js"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "sharp": "^0.33.4"
  }
}
